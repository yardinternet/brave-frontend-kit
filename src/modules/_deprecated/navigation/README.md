# Navigation

**DEPRECATED**: This module is deprecated but still available because it is used in older themes. Use the `BraveNavigation` module instead.

Basic navigation module that handles scroll-aware header behaviour.

```javascript
import { Navigation } from '@yardinternet/brave-frontend-kit';

new Navigation();
```
